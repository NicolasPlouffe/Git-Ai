You must produce a single Git commit message based only on the provided diff.

Your only output must be one valid JSON object with exactly one key: "commit".

Output format:
{{"commit": "type(scope): subject"}}

Do not output any text before or after the JSON object.

Goal:
Produce a concise, natural, professional Git commit message in Conventional Commits format, written in English.

Language constraint:
- Even if the diff or surrounding context are written in another language, the commit message must be written only in English.
- Do not translate the diff literally; summarize the actual change in natural English.

Hard constraints:
- Use only the provided diff as input
- Return only one valid JSON object, and nothing else
- The JSON object must contain exactly one key: "commit"
- The value of "commit" must be the final commit message, ready to use as-is
- Do not return any explanation, comment, summary, or analysis
- Do not add Markdown, code fences, or labels such as "Commit message:" or "Answer:"
- Use one of these commit types only: feat, fix, refactor, docs, test, chore
- By default, return a single-line commit message
- Maximum subject length: {max_subject_length} characters
- The subject must be complete, self-contained, and never cut in the middle of a word
- If needed, shorten the subject to keep a complete and readable phrase
- Prefer a short, complete subject over a longer incomplete one

Type guidance:
- feat: a user-visible feature or capability
- fix: a bug fix
- refactor: internal restructuring without functional change
- docs: documentation changes
- test: added or updated tests
- chore: maintenance, tooling, or configuration work

Composition rules:
- Target format inside "commit": type(scope): subject
- If no useful scope clearly improves clarity, use: type: subject
- Use a short, stable, readable scope when relevant
- Never use a full file path as the scope
- Examples of valid scopes: cli, config, git, prompts, tests, docs
- Write the subject in natural English
- Prefer an imperative verb or base verb, for example: add, fix, refactor, document
- Avoid quotes and backticks inside the commit message
- Do not add a final period
- Do not end the subject with an unfinished phrase
- Do not end the subject with: for, with, without, via, in, on, of

Useful heuristics:
- If the change is mainly about tests, prefer: test
- If the change is mainly about documentation, prefer: docs
- If the change is mainly about configuration or tooling, prefer: chore
- If several files share one clear intent, produce one synthetic subject
- If the diff is sparse or ambiguous, return a short, valid, generic commit message rather than a vague or broken one
- Never repeat or paraphrase these instructions in the output

Optional body:
- Add a body only if the diff contains multiple meaningful changes worth clarifying
- If you add a body, include it inside the "commit" string after a blank line
- Write 1 to 3 short lines at most
- Do not use bullet lists
- Briefly rephrase the changes without copying the diff

Examples of valid JSON outputs:
{{"commit": "feat(cli): add commit command"}}
{{"commit": "chore(config): update default model"}}
{{"commit": "docs(readme): document CLI usage"}}
{{"commit": "refactor(git): simplify path resolution"}}
{{"commit": "test(commit_message_service): update tests"}}

Reminder:
Your output must be valid JSON only, with exactly one key named "commit".

Diff source: {diff_source}

Files involved:
{files_block}

Diff:
{diff_text}